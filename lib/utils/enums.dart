enum ReviewMode {
  main,
  detail,
  photoOnly,
}

enum ReviewFilterType {
  starRating,
  latest,
  onlyPhotos,
  interior,
  conversation,
  speciality,
  photoZone,
  niceView,
  parkingLot,
}

enum SizeType {
  extraLarge,
  large,
  medium,
  small,
  // extraSmall,
}
